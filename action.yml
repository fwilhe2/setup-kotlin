name: 'Setup Kotlin'
description: 'Install the Kotlin compiler'
author: 'Florian Wilhelm'
branding:
  icon: 'code'
  color: 'orange'
inputs:
  script:
    required: false
    description: 'Script to execute'
runs:
  using: 'node12'
  main: 'dist/index.js'
